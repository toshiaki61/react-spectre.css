import React from 'react';
import cx from 'classnames';
var NavbarSection = function (_a) {
    var children = _a.children, className = _a.className;
    return (<section className={cx('navbar-section', className)}>{children}</section>);
};
export default NavbarSection;
//# sourceMappingURL=NavbarSection.jsx.map