import React from 'react'

import {ParallaxPartProps} from './interfaces'

const ParallaxFront = ({children}: ParallaxPartProps) => (
  <div className="parallax-front">{children}</div>
)

export default ParallaxFront
