import React from 'react';
import cx from 'classnames';
var PopoverContainer = function (_a) {
    var children = _a.children, className = _a.className;
    return (<div className={cx('popover-container', className)}>{children}</div>);
};
export default PopoverContainer;
//# sourceMappingURL=PopoverContainer.jsx.map