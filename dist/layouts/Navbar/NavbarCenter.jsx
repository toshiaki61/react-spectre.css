import React from 'react';
import cx from 'classnames';
var NavbarCenter = function (_a) {
    var children = _a.children, className = _a.className;
    return (<section className={cx('navbar-center', className)}>{children}</section>);
};
export default NavbarCenter;
//# sourceMappingURL=NavbarCenter.jsx.map